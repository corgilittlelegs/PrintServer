--- /tmp/thread.c.orig
+++ cupsraster/cups/thread.c
@@ -154,7 +154,9 @@
 void
 _cupsThreadCancel(_cups_thread_t thread)/* I - Thread ID */
 {
-  pthread_cancel(thread);
+  /* Bionic (Android's libc) has no pthread_cancel. Unused here — nothing in
+   * this build calls _cupsThreadCancel — so a no-op keeps this TU linking. */
+  (void)thread;
 }
 
 
